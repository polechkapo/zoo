const React = require('react');
const Layout = require('./Layout');

module.exports = function AdminCabinet({ animal }) {
  return (
    <Layout>
      <div className="cab">
        <p>привет</p>
      </div>
    </Layout>
  );
};
